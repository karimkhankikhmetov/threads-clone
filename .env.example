# Generate a strong secret with: openssl rand -hex 32
SECRET_KEY=replace-this-with-a-long-random-string

# Comma-separated allowed origins for CORS. Use * for demo, your domain for production.
ALLOWED_ORIGINS=*
